#include <vector>

#include "image.h"

using namespace tempura;
auto main() -> int {
  // Kitty image, greyscale xy gradiant
  std::vector<GreyScalePixel> pixels;
  pixels.reserve(100 * 100);

  return 0;
}
