# matrix4 — linear-algebra module: the Dense owner (in matrix.h) + Matrix/Vec
# aliases over it. tempura_test() is defined in the parent src/CMakeLists.txt.
tempura_test(matrix      LABEL base)
tempura_test(vec         LABEL base)
tempura_test(permutation LABEL base)
tempura_test(permuted    LABEL base)
