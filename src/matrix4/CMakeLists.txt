# matrix4 — linear-algebra module: MdArray storage, Matrix/Vec aliases over it.
# tempura_test() is defined in the parent src/CMakeLists.txt (functions are global).
tempura_test(mdarray LABEL base)
tempura_test(matrix  LABEL base)
tempura_test(vec     LABEL base)
